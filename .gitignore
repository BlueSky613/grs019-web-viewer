# Publish only the self-contained webviewer app. Everything else in this working
# directory (full viewerData incl. 563 MB points/, previewer, scratch) stays LOCAL
# and is never committed.
/*
!/.gitignore
!/vercel.json
!/webviewer/
